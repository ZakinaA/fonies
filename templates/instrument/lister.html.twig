<!DOCTYPE html>
<html>
    <head>

    </head>
    <body>

		LISTE DES INSTRUMENT</br>
		<table>
		{% for i in pInstruments %}
		<tr>
			<td>{{ i.id }}</a></td>
            <td>{{ i.intitule}}</td>
			<td>{{ i.dateAchat|date('d/m/Y') }}</td>

	{% else %}
		<tr>
		<td>Aucun etudiant n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
	 
    </body>
</html>